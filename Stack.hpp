#include <vector>

namespace ft
{


template <class T, class Container = std::vector<T>> 
class stack
{
public:
	// Constructeur
	// Destructeur
	// Operator ==
	// Top
	// Empty
	// Size
	// Push
	// Pop
private:

}; // Class Vector

// Fonctions non membre :
// == != < <= > >=

} // Namespace FT