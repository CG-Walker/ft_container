- Vérifier le binz avec value_compare (Comprendre la classe)
- Mettre au propre
- Tout tester
  
- Comment accèder aux valeurs avec les iterators ? - Done
- reverse_iterator n'a pas l'air de fonctionner - Done
- Vérifier la valeur de max_size()
- Tester toutes les fonctions insert()
  - Comprendre le insert avec hint
- Erase() ne fonctionne pas - Done
- clear() crash lorsque c'est appelé sur une map vide - Done
- count() ne fonctionne pas
- equal_range(), lower_bound() et upper_bound() n'ont pas l'air de fonctionner

